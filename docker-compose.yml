version: "3"
services:
  dice_maiden:
    build:
      context: .
      dockerfile: Dockerfile
    image: theforgedconcordance/dicemaiden:tfc
    container_name: dicemaiden
    volumes:
      - .env:/opt/DiceMaiden/.env
